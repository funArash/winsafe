window.SIDEBAR_ITEMS = {"struct":[["CloseClipboardGuard","RAII implementation for clipboard which automatically calls `CloseClipboard` when the object goes out of scope."],["CloseHandleGuard","RAII implementation for a `Handle` which automatically calls `CloseHandle` when the object goes out of scope."],["CloseHandlePiGuard","RAII implementation for `PROCESS_INFORMATION` which automatically calls `CloseHandle` on `hProcess` and `hThread` fields when the object goes out of scope."],["CloseThemeDataGuard","RAII implementation for `HTHEME` which automatically calls `CloseThemeData` when the object goes out of scope."],["CoUninitializeGuard","RAII implementation which automatically calls `CoUninitialize` when the object goes out of scope."],["DeleteDCGuard","RAII implementation for `HDC` which automatically calls `DeleteDC` when the object goes out of scope."],["DeleteObjectGuard","RAII implementation for a `GdiObject` which automatically calls `DeleteObject` when the object goes out of scope."],["EndDeferWindowPosGuard","RAII implementation for `HDWP` which automatically calls `EndDeferWindowPos` when the object goes out of scope."],["EndPaintGuard","RAII implementation for `HDC` which automatically calls `EndPaint` when the object goes out of scope."],["EndUpdateResourceGuard","RAII implementation `HUPDATERSRC` which automatically calls `EndUpdateResource` when the object goes out of scope."],["FindCloseGuard","RAII implementation for `HFINDFILE` which automatically calls `FindClose` when the object goes out of scope."],["FreeLibraryGuard","RAII implementation for `HINSTANCE` which automatically calls `FreeLibrary` when the object goes out of scope."],["GlobalFreeGuard","RAII implementation for `HGLOBAL` which automatically calls `GlobalFree` when the object goes out of scope."],["ImageListEndDragGuard","RAII implementation for imagelist drag which automatically calls `ImageList_EndDrag` when the object goes out of scope."],["RegCloseKeyGuard","RAII implementation for `HKEY` which automatically calls `RegCloseKey` when the object goes out of scope."],["ReleaseCaptureGuard","RAII implementation for `HWND` which automatically calls `ReleaseCapture` when the object goes out of scope."],["ReleaseDCGuard","RAII implementation for `HDC` which automatically calls `ReleaseDC` when the object goes out of scope."],["SelectObjectGuard","RAII implementation for `HDC::SelectObject` calls, which automatically selects the previous GDI object at the end of the scope."],["UnlockFileGuard","RAII implementation for the `HFILE` lock which automatically calls `UnlockFile` when the object goes out of scope."],["UnmapViewOfFileGuard","RAII implementation for `HFILEMAPVIEW` which automatically calls `UnmapViewOfFile` when the object goes out of scope."]]};